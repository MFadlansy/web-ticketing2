package com.example.web_tiket.model;

// Enum untuk mendefinisikan peran (roles) pengguna dalam aplikasi.
public enum Role {
    USER,  // Peran untuk pengguna biasa
    ADMIN  // Peran untuk administrator
}